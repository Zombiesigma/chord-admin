import {NextResponse} from 'next/server';
export async function POST(req:Request){try{const token=process.env.GITHUB_TOKEN,owner=process.env.GITHUB_OWNER,repo=process.env.GITHUB_REPO,branch=process.env.GITHUB_BRANCH||'main',base=process.env.GITHUB_COVERS_PATH||'covers';if(!token||!owner||!repo)return NextResponse.json({error:'GitHub storage belum dikonfigurasi di environment.'},{status:500});const form=await req.formData();const file=form.get('file');if(!(file instanceof File))return NextResponse.json({error:'File tidak ditemukan.'},{status:400});if(!file.type.startsWith('image/'))return NextResponse.json({error:'Hanya file gambar.'},{status:400});if(file.size>8*1024*1024)return NextResponse.json({error:'Ukuran maksimum 8 MB.'},{status:400});const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'')||'jpg';const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,'-').replace(/-+/g,'-');const path=`${base}/${Date.now()}-${safe || `cover.${ext}`}`;const bytes=Buffer.from(await file.arrayBuffer());const api=`https://api.github.com/repos/${owner}/${repo}/contents/${path}`;const gh=await fetch(api,{method:'PUT',headers:{Authorization:`Bearer ${token}`,Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','Content-Type':'application/json'},body:JSON.stringify({message:`Add cover ${safe}`,content:bytes.toString('base64'),branch})});const data=await gh.json();if(!gh.ok)return NextResponse.json({error:data.message||'GitHub upload gagal.'},{status:gh.status});return NextResponse.json({url:`https://raw.githubusercontent.com/${owner}/${repo}/${branch}/${path}`,path});}catch(e:any){return NextResponse.json({error:e.message||'Upload error'},{status:500})}}
