'use client';
import Link from 'next/link';import {usePathname,useRouter} from 'next/navigation';import {LayoutDashboard,Music,Users,Disc3,LogOut} from 'lucide-react';import {signOut} from 'firebase/auth';import {auth} from '@/lib/firebase';
export default function Sidebar(){const p=usePathname(),r=useRouter();const items=[['/dashboard','Dashboard',LayoutDashboard],['/songs','Lagu',Music],['/bands','Band',Users],['/albums','Album',Disc3]] as const;return <aside className="sidebar"><div className="brand">CHORD<span> ADMIN</span></div><nav className="nav">{items.map(([href,label,Icon])=><Link className={p===href?'active':''} href={href} key={href}><Icon size={17}/><span>{label}</span></Link>)}<button onClick={async()=>{await signOut(auth);r.replace('/login')}}><LogOut size={17}/><span>Keluar</span></button></nav></aside>}
